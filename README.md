# vietngoquang.com

Source of my personal website. Each version lives on its own branch; this branch only holds this index.

| Branch | Notes |
|--------|-------|
| `v2.0` | Current site. Plain HTML/CSS/JS, content in `content/*.json`. |
| `v1.0` | Previous site, built with Next.js. |

GitHub Pages deploys from the latest version branch.
